// Import Actions
import {  } from './HomeActions';

// Initial State
const initialState = {};

const HomeReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default HomeReducer;
